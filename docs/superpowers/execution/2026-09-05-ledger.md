# Website modernization execution

Plan: ../plans/2026-09-05-genefoundry-modernization.md
Baseline: 53d6ce27f1c60c2fb0422b2f05f9752e3dce8b77.
Implementation checkout: .worktrees/website-modernization, branch feat/website-modernization.

- Baseline production build and type-check passed. Old artifact preserved under ignored .build/baseline/.
- G0 in progress: exact shared contracts and test harness. A/B/C assigned exclusive plan ownership; coordinator owns integration.
- Generated audits/research/tool state ignored by user request; source specs/plans and sanitized verification remain versionable.
- Execution adaptation: lane workers author their final owned files directly after documented contract freeze; coordinator avoids touching those files to prevent conflicts. Final publication validation cannot bypass missing/fixture content.
- All six client guides initially documentation-only; authenticated client execution has not been performed.
- G0 URL tests: missing-module failure observed before implementation, then 2/2 pass. Content contracts reject missing joins, duplicate routes, forged recipe states and invalid dates.
- A reports filter tests 3/3 and scoped lint passing; B reports content/contracts 6/6. Integrated unit suite currently 16/16. Full type check waits on B-owned page files.
- Independent A review of C core: no material source-level defects in fresh SSR instances, shared build year, prerender/PWA ordering, no-fallback 404, Docker browser isolation or private evidence boundary. Runtime artifact/HTTP/worker gates remain pending.
- A final scoped browser suite 10/10; B browser suite 9/9. Root cross-page automated WCAG checks at390/1440 and200%text at320 all passed.
- A independently reviewed B publication states, legal preservation and clipboard behavior: no material gaps; requested light focus outline in dark CommandCard to match the approved design. B owns final fix.
- C root/mirror artifacts validated37documents each; independent static browser suite82/82 passed including no-JS, hydration, worker offline/backend boundaries and previous-worker upgrade. Actual Docker builder evidence-context fixture passed.
- Final Impeccable scan: four duplicate Inter font-family warnings. Approved Inter body retained alongside Archivo headings; no decorative-pattern rewrite warranted.

## Final revision supersedes the initial checkpoints

The initial six-client, illustrative-workflow and Inter-font checkpoints above describe an earlier implementation. The final website has seven client guides, two MCP-executed workflows, Source Sans 3 and Archivo, consistent full-width pages, and a zero-finding Impeccable scan. Browser setup instructions are documented, not claimed as fresh authenticated setup tests.

Upstream `origin/main` security and release changes were integrated rather than overwritten: pinned actions and container bases, release attestations, deployment environment validation and current dependency overrides remain. Node 24 and Vite 8 are used throughout. A compatible fast-uri patch removed the remaining dependency advisory.

The full production audit covers all 37 indexable routes, plus separate 404 and HTTP tests. See the final performance review for the measured build and limits. Generated reports, raw MCP logs, screenshots, dependency folders and build outputs stay ignored; sanitized workflow captures remain versionable.

## Final local integration verification — 6 September 2026

The user approved the continuous contour animation. The final tree passed formatting, TypeScript, ESLint, 36 unit tests, 8 Node release tests and the compose/workflow shell checks. The complete browser pipeline passed 75 development tests, 92 root/mirror static tests and 3 production nginx tests. Both production bases contain 38 prerendered documents. Repeated per-page development audits use a fresh browser context per page; separate real navigation assertions retain coverage of transitions and classic scrollbar alignment.

All 37 indexable production routes scored 99–100 mobile performance and 100 accessibility, best practices and SEO, with zero measured CLS. The approved animated homepage scored 100 in all desktop categories. These are local lab measurements; the mirror intentionally remains noindex. See [the maintained performance review](performance/final-review.md). Impeccable reported no findings. Generated reports and private execution traces remain ignored.

A read-only integration review found no unresolved merge entries, conflict markers or release/security regressions; all 23 GitHub Action references remain pinned. Local CI equivalents were executed; remote CI and publication were not triggered.

## Final 100-point performance revision — 6 September 2026

The remaining 99-point mobile pages were improved by embedding the small generated stylesheet in prerendered HTML and using build-time Brotli quality 11. A fresh, sequential production nginx Lighthouse audit verified all 37 indexable routes at 100 performance, accessibility, best practices and SEO, with zero CLS and maximum LCP 1.77 seconds. The approved animation remains unchanged. The mirror retains its deliberate duplicate-content noindex policy; 404 responses also remain non-indexable.

The stylesheet change passed all 170 browser tests across development, root/mirror static artifacts and production nginx, plus 37 unit tests and 8 release tests. The final compression change passed byte round-trip tests and both complete artifact validations. New regressions verify embedded CSS for both bases, no blocking stylesheet links on every indexable page, and rejection of escaping stylesheet paths/HTML-closing content. Detailed per-route measurements and implementation tradeoffs are in the maintained performance review.
