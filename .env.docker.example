# DOCKER DEPLOYMENT .env.docker settings
# ======================================
# INSTRUCTIONS:
# 1. Copy this file to '.env.docker' in the same directory: `cp .env.docker.example .env.docker`
# 2. Edit '.env.docker' with your specific settings
# 3. This file will be used by docker-compose.yml when you run the services.

# --- Docker Network Configuration ---
# Name of the Docker network that Nginx Proxy Manager (NPM) uses.
# If your NPM was started via docker-compose from a directory named 'npm',
# this is typically 'npm_default'. Verify with 'docker network ls' on your server.
NPM_SHARED_NETWORK_NAME=npm_default

# --- Local Development (Optional) ---
# Port to expose when running without NPM (for local testing)
# Only used by docker-compose.yml, not docker-compose.npm.yml
GENEFOUNDRY_PORT=8080

# --- Self-Hosted Plausible Analytics ---
# Domain where your self-hosted Plausible instance is running
# Used in Content-Security-Policy header (nginx.conf must be updated if changed)
PLAUSIBLE_DOMAIN=plausible.genefoundry.org
